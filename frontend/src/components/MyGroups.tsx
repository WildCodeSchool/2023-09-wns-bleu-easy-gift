

/*
function MyGroups({
  groupCards,
}: {
  groupCards: {
    _typename?: "userGroup" | undefined;
    group_title: string,
  }
}) {
  return (
    <div>
      {groupCards.map((card) => (

      ))}
    </div>
  )
}

export default MyGroups;

 */

function MyGroups() {
  return (
    <div>

    </div>
  );
}

export default MyGroups;
